spring.application.name=identity

jwt.secret = a1f4b9c2d75e3f8a6e1b9cd0f3471e89a023c17de8fa2bd6c57c1a3e90d4f2b1=
jwt.expiration= 86400000

spring.datasource.url=jdbc:mysql://localhost:3306/identity_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=password

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=8080

# Verifiable Credential signing secret (HMAC-SHA256)
vc.secret=${VC_SECRET:vc-secret-trustid-org1-2026}

# Issuer DID — used in VC issuer + verificationMethod fields
vc.issuer-did=did:fabric:trustid:org1

# W3C Status List 2021 — VC revocation
vc.status-list.id=employment-status-list-1
vc.status-list.size=131072
vc.status-list.base-url=http://localhost:8080/api/v1/status-list

# SD-JWT Selective Disclosure (separate secret so a leak doesn't compromise legacy VCs)
sd-jwt.secret=${SD_JWT_SECRET:sd-jwt-secret-trustid-org1-2026}